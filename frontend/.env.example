VITE_API_URL=http://localhost:4000

# Google Sign-In — create an OAuth 2.0 Client ID (Web application type) in
# Google Cloud Console > APIs & Services > Credentials. Add your dev/prod
# origins to "Authorized JavaScript origins". Without this, the Google
# button renders disabled with an explanatory tooltip.
VITE_GOOGLE_CLIENT_ID=
